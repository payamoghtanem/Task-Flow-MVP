# TaskFlow Frontend — Environment Variables
# Copy to .env and update values. Never commit .env to git.

# API base URL — in dev the Vite proxy forwards /api/* to localhost:3001
# In production set this to your deployed backend URL
VITE_API_URL=
